@{
    ViewData["Title"] = "Takvim Yönetimi";
}

<div class="ai-form-container">
    <h1>@ViewData["Title"]</h1>
    <p>Gelecek 4 haftanın derslerini, kayıtlı ders şablonlarına göre otomatik olarak oluşturun.</p>

    @if (TempData["Basari"] != null)
    {
        <div class="alert alert-success">@TempData["Basari"]</div>
    }
    @if (TempData["Bilgi"] != null)
    {
        <div class="alert alert-info">@TempData["Bilgi"]</div>
    }

    <form asp-action="HaftalikTakvimOlustur" method="post">
        <p>
            Bu işlem, mevcut ders programı şablonlarını (Pzt 10:00 Yoga vb.) kullanarak
            gelecek 4 hafta için takvime gerçek, kayıt olunabilir sınıflar ekler.
            Eğer bir ders zaten takvimde varsa, mükerrer kayıt oluşturmaz.
        </p>
        <input type="submit" value="Gelecek 4 Haftalık Takvimi Oluştur/Güncelle" class="btn btn-primary btn-lg" />
    </form>

    <hr />

</div>